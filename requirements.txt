# Core Django
Django>=6.0.2,<6.1  # Updated to 6.0.2+ for security fixes (SQL injection, timing attack)
djangorestframework==3.15.*
django-filter==25.*

# Database
mysqlclient==2.2.*

# Security
argon2-cffi==23.1.*
django-axes==6.1.*
django-ratelimit==4.1.*
cryptography>=46.0.5,<47.0.0  # Updated to 46.0.5+ to fix subgroup attack vulnerability (SECT Curves)
pip-audit==2.7.*

# 2FA
django-two-factor-auth[phonenumbers]==1.16.*
qrcode==7.4.*
pyotp==2.9.*

# WSGI Server
gunicorn==23.0.*  # Updated from 22.0.* to fix CVE-2024-6827 (HTTP Request Smuggling)
whitenoise==6.6.*

# PSA Integration HTTP clients
requests>=2.33.0,<2.34  # Updated to 2.33.0+ to fix Insecure Temp File Reuse in extract_zipped_paths()
urllib3==2.6.*  # Updated from 2.1.* to fix SNYK-PYTHON-URLLIB3-14192442 (Data Amplification)

# AI Integration
anthropic==0.75.*

# Utilities
python-dotenv>=1.2.2,<1.3  # Updated to 1.2.2+ to fix symlink-following arbitrary file overwrite in set_key
Pillow>=12.2.0,<13.0  # Updated to 12.2.0+ to fix FITS GZIP decompression bomb (was 12.1.* for PSD oob-write)
markdown>=3.8.1,<4.0  # Updated to 3.8.1+ to fix uncaught-exception DoS
bleach==6.1.*
pytz==2024.1
psutil==5.9.*
packaging==24.1  # Version comparison for auto-updates

# Azure AD / Microsoft Entra ID Integration
msal==1.34.*
requests-oauthlib==1.3.*

# Note: LDAP/Active Directory support moved to requirements-optional.txt
# See: https://github.com/agit8or1/clientst0r/blob/main/requirements-optional.txt
